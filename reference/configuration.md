---
title: "Configuration"
description: "List every setting webrzn accepts, with its type, default and what happens when it is set wrongly."
status: generated
version: "0.1"
---

# Configuration

One row per setting. Keep the columns identical on every reference page so a reader learns the shape once.

| Setting | Type | Default | What it does |
| --- | --- | --- | --- |
| — | — | — | — |

Two columns people leave out and then answer by hand: what happens when the value is wrong, and whether a change takes effect immediately or on restart.
